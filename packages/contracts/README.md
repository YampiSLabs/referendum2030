# API Contracts

This package stores the public API contract for the Referendum 2030 demo.

- `openapi.json` is a snapshot of the Django Ninja API.
- Regenerate it after API changes from Django Ninja when the backend is running.

